function btnAnimar(){
    const btn = document.getElementById("btn-menu");
    btn.classList.toggle('active');
    //Esse toggle serve para verificar se
    //ja existe ou não a class active
    // se existir ele remove se não existir
    // ele adiciona a class active
}